// Re-export for backwards compatibility. Canonical source is now
// lib/messaging/stage-meta.ts so the GHL widget bundle stays standalone
// after the dashboard is retired.
export { STAGE_LABEL, STAGE_TONE, timeAgoHe } from "@/lib/messaging/stage-meta";
