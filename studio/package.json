{
  "name": "albadi-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local Bag Studio — a Claude-backed page to make mockups (הדמיה) + dielines (פריסה) and push them to the CRM. Runs ONLY on Eli's Mac (needs the local skills + Gemini/Veo keys). Isolated node_modules so its zod v4 never touches the CRM's zod v3.",
  "scripts": {
    "start": "tsx server.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.218",
    "tsx": "^4.19.0"
  }
}
